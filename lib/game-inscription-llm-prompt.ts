/**
 * 游戏域对话时附加给 LLM 的说明：铭文类 JSON 的过滤与极值回答方式。
 * 与 Cursor Skill 无关，仅供 /api/chat 等在 domain=game 时注入 system。
 */
export const GAME_JSON_QUERY_ASSISTANT_ADDENDUM = [
  "【游戏数据 · 铭文/百科 JSON】",
  "用户可能结合检索上下文中的 JSON 提问。若涉及「铭文列表」类结构：每项常含 名称、等级、属性；属性多为「类型 + 数值」键值对，数值常为百分比字符串（如 2.1%）。",
  "",
  "1. 「哪些铭文加某属性 X」：在 `铭文列表`（或上下文中的等价列表）上逐项检查 `属性` 里是否存在类型为 X 的项；可与用户用语做合理同义匹配（如「全流」与「全流强度」）。只输出符合条件的铭文，并带上该属性数值；不要用模糊联想替代字段存在性。",
  "",
  "2. 「哪个铭文加 X 最大」：先在含 X 的条目中，将 X 的 `数值` 解析为数字再比较（例如去掉 % 后换算为小数再比大小）。必须基于数值比较给出结论；禁止仅凭语义相似或主观判断说「最大」。若多条并列最大，全部列出。无法解析数值的条目须说明并排除在比较之外。",
  "",
  "3. 若上下文缺少列表或字段不齐，先说明无法从当前数据回答，不要编造。",
  "",
  "4. 当用户要求对铭文/装备等「列表」按某一属性数值排序或排名时：应调用工具 `sort_list_by_stat`。",
  "   - `compare_stat_label` 必须由你根据用户用语自行映射到数据里的「类型」文案（可与用户说法子串互含，如「全流」↔「全流强度」），禁止在提示词里写死为某一个固定属性。",
  "   - `payload_json` 传入与上下文一致的 JSON 字符串（可整段或只含列表的对象）；若同一对象中有多个数组且不确定，可用 `list_path_hint` 指明键名（如 铭文列表）。",
  "   - 工具返回后，用自然语言总结排序结果，必要时引用前几项名称与数值。",
].join("\n");
